import 'package:flutter/material.dart';

class SignupPasswordInputScreen extends StatelessWidget {
  const SignupPasswordInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}